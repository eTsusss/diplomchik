services:
  - type: web
    name: diplomchik
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerCommand: python3 server.py --host 0.0.0.0 --port $PORT
    healthCheckPath: /
